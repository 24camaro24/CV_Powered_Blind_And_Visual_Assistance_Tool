# GPU-oriented install for this project (Windows/Linux, Python 3.10+).
# Use this when you want CUDA-backed inference for PyTorch + ONNX.
#
# Usage:
#   python -m pip install -U pip setuptools wheel
#   python -m pip install -r requirements-gpu.txt
#
# Notes:
# - Torch CUDA wheels are installed from the PyTorch CUDA index.
# - ONNX runtime uses the GPU package here.
# - System CUDA/cuDNN runtime compatibility is still required on host machine.

--extra-index-url https://download.pytorch.org/whl/cu124

Flask>=2.3.0
flask-cors>=4.0.0
Werkzeug>=2.3.0

# CUDA-enabled PyTorch stack
torch==2.5.1+cu124
torchvision==0.20.1+cu124
torchaudio==2.5.1+cu124

# Core ML deps
numpy>=1.22.0,<2
opencv-python>=4.8.0.0
Pillow>=10.0.0
transformers>=4.33.0,<5
depth-anything-3>=0.1.1
faster-whisper>=0.9.0
TTS>=0.22.0
groundingdino-py>=0.4.0
rembg>=2.0.67

# CUDA-capable ONNX runtime
onnxruntime-gpu==1.23.2
